# vLLM serving config for causalpool-4B-DESC

defaults:
  - default

model: null  # Will be replaced with model_path
model_path: "outputs/sft/ds2-desc/checkpoint-346/merged"
served_model_name: "CausalPool-4B-desc"

